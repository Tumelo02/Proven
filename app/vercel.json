{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": [
    "Vercel's Root Directory for this project is `app`, with 'Include files",
    "outside the root directory' switched on. That is what lets Vercel find",
    "`next` in app/package.json: it scans the root directory's manifest, and",
    "the repository root is a workspace container that does not depend on next",
    "itself.",
    "",
    "The install and build commands still run from the repository root, because",
    "@proven/engine has to be compiled to dist/ before the app can import it,",
    "and dist/ is not committed."
  ],

  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "trailingSlash": false,

  "redirects": [
    { "source": "/index.html", "destination": "/", "permanent": false },
    { "source": "/dashboard.html", "destination": "/platform", "permanent": false },
    { "source": "/entrepreneur", "destination": "/platform", "permanent": false },
    { "source": "/funder", "destination": "/platform", "permanent": false }
  ],

  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "DENY" },
        {
          "key": "Permissions-Policy",
          "value": "camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()"
        },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'none'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https://*.supabase.co; font-src 'self' data:; connect-src 'self' https://*.supabase.co wss://*.supabase.co; form-action 'self'; frame-ancestors 'none'; base-uri 'none'; object-src 'none'; upgrade-insecure-requests"
        }
      ]
    },
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
